apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "face-recognition.fullname" . }}
spec:
  to:
    kind: Service
    name: {{ include "face-recognition.fullname" . }}
  port:
    targetPort: 8080
  tls:
    termination: edge
