apiVersion: v1
kind: Service
metadata:
  name: {{ include "face-recognition.fullname" . }}
spec:
  ports:
    - port: 8080
      targetPort: {{ .Values.service.port }}
  selector:
    app: {{ include "face-recognition.name" . }}
